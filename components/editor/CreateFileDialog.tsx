'use client';import {useState} from 'react';import {Modal} from '../ui/Modal';export function CreateFileDialog({open,onClose,onCreate}:any){const[p,setP]=useState('');return <Modal open={open} onClose={onClose} title='Create file'><input className='input' value={p} placeholder='src/example.ts' onChange={e=>setP(e.target.value)}/><button className='btn btn-primary' onClick={()=>onCreate(p)}>Create</button></Modal>}
