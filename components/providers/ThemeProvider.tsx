'use client';import {createContext,useContext,useEffect,useState} from 'react';const C=createContext({dark:false,toggle:()=>{}});export function ThemeProvider({children}:any){const[dark,setDark]=useState(false);useEffect(()=>{document.documentElement.dataset.theme=dark?'dark':'light'},[dark]);return <C.Provider value={{dark,toggle:()=>setDark(x=>!x)}}>{children}</C.Provider>}export const useTheme=()=>useContext(C);
