'use client';import {Modal} from '../ui/Modal';export function ZipDialog({open,onClose,files}:any){async function go(){const d=await(await fetch('/api/zip',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({files})})).json();if(d.base64){const a=document.createElement('a');a.href='data:application/zip;base64,'+d.base64;a.download=d.filename;a.click()}}return <Modal open={open} onClose={onClose} title='Export ZIP'><p>{files?.length||0} files</p><button className='btn btn-primary' onClick={go}>Create ZIP</button></Modal>}
