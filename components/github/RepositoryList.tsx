'use client';import {useEffect,useState} from 'react';import {RepositoryCard} from './RepositoryCard';export function RepositoryList(){const[d,setD]=useState<any[]>([]);const[e,setE]=useState('');useEffect(()=>{fetch('/api/github/repos').then(r=>r.json()).then(x=>Array.isArray(x)?setD(x):setE(x.error||'Failed'))},[]);return e?<div className='error-box'>{e}</div>:<div className='repo-grid'>{d.map(r=><RepositoryCard key={r.id} repo={r}/>)}</div>}
