import {NextRequest,NextResponse} from 'next/server';import {githubFetch} from '@/lib/github/server';export async function GET(q:NextRequest){const s=q.nextUrl.searchParams.get('q');if(!s)return NextResponse.json({error:'q required'},{status:400});try{return NextResponse.json(await githubFetch(`/search/code?q=${encodeURIComponent(s)}`))}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
