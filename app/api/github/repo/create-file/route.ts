import {NextRequest,NextResponse} from 'next/server';import {githubFetch} from '@/lib/github/server';export async function POST(req:NextRequest){try{const b=await req.json();const {owner,repo,path,content,branch,sha,message}=b;if(!owner||!repo||!path||content===undefined)return NextResponse.json({error:'missing required fields'},{status:400});const body:any={message:message||`Update ${path}`,content:Buffer.from(content,'utf8').toString('base64'),branch:branch||'main'};if(sha)body.sha=sha;return NextResponse.json(await githubFetch(`/repos/${owner}/${repo}/contents/${path}`,{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)}))}catch(e:any){return NextResponse.json({error:e.message},{status:500})}}
