import {NextRequest,NextResponse} from 'next/server';import {githubFetch} from '@/lib/github/server';export async function GET(q:NextRequest){const o=q.nextUrl.searchParams.get('owner'),r=q.nextUrl.searchParams.get('repo');if(!o||!r)return NextResponse.json({error:'missing params'},{status:400});try{return NextResponse.json(await githubFetch(`/repos/${o}/${r}`))}catch(e:any){return NextResponse.json({error:e.message},{status:404})}}
