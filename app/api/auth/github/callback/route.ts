import {NextRequest,NextResponse} from 'next/server';import {exchangeCode} from '@/lib/auth/oauth';import {encodeSession,SESSION_COOKIE} from '@/lib/auth/session';
export async function GET(req:NextRequest){const code=req.nextUrl.searchParams.get('code');if(!code)return NextResponse.redirect(new URL('/login?error=missing_code',req.url));try{const t=await exchangeCode(code);const u=await fetch('https://api.github.com/user',{headers:{Authorization:`Bearer ${t.access_token}`,Accept:'application/vnd.github+json'}});if(!u.ok)throw Error('User lookup failed');const user=await u.json();const r=NextResponse.redirect(new URL('/dashboard',req.url));r.cookies.set(SESSION_COOKIE,encodeSession({login:user.login,avatar_url:user.avatar_url,name:user.name,token:t.access_token}),{httpOnly:true,sameSite:'lax',secure:process.env.NODE_ENV==='production',maxAge:604800,path:'/'});return r}catch{return NextResponse.redirect(new URL('/login?error=oauth_failed',req.url))}}
