import {NextRequest,NextResponse} from 'next/server';import JSZip from 'jszip';import {validateZipFiles} from '@/lib/zip/fileLimit';export async function POST(req:NextRequest){try{const b=await req.json();const files=b.files||[];validateZipFiles(files);const z=new JSZip();for(const f of files)if(f.path)z.file(f.path,String(f.content??''));const base64=await z.generateAsync({type:'base64'});return NextResponse.json({base64,filename:b.filename||'bluegit-project.zip'})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
