import {getSession} from '@/lib/auth/session';import {redirect} from 'next/navigation';import Link from 'next/link';export default async function Settings(){if(!(await getSession()))redirect('/login');return <main className='page'><h1>Settings</h1><div className='action-grid'><Link className='action-card' href='/settings/account'>Account<span>Profile and logout</span></Link><Link className='action-card' href='/settings/appearance'>Appearance<span>Light/dark mode</span></Link></div></main>
