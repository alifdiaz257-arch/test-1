import {getSession} from '@/lib/auth/session';import {redirect} from 'next/navigation';import {Avatar} from '@/components/ui/Avatar';import {UserMenu} from '@/components/layout/UserMenu';export default async function Account(){const s=await getSession();if(!s)redirect('/login');return <main className='page'><h1>Account</h1><div className='panel account'><Avatar src={s.avatar_url} name={s.login}/><div><b>{s.name||s.login}</b><p>@{s.login}</p></div><UserMenu/></div></main>}
