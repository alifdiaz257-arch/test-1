import {redirect} from 'next/navigation';import {getSession} from '@/lib/auth/session';import {RepositoryList} from '@/components/github/RepositoryList';export default async function Dashboard(){const s=await getSession();if(!s)redirect('/login');return <main className='page'><section className='hero'><span className='eyebrow'>GITHUB WORKSPACE</span><h1>Welcome, {s.name||s.login}</h1><p>Open a repository and edit files directly from BlueGit.</p></section><h2>Your repositories</h2><RepositoryList/></main>}
