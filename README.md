# BlueGit Editor

Next.js GitHub repository editor. Configure the GitHub OAuth App callback as `/api/auth/github/callback`, then set the environment variables from `.env.example`.

Features: GitHub OAuth, repositories, branches, recursive file tree, file editing, create/update/delete commits, commit history, search, ZIP export and responsive blue/white UI.
