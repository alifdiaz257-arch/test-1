export function githubAuthorizeUrl(state:string){const p=new URLSearchParams({client_id:process.env.GITHUB_CLIENT_ID||'',redirect_uri:process.env.GITHUB_REDIRECT_URI||'',scope:'read:user user:email repo',state});return 'https://github.com/login/oauth/authorize?'+p}
export async function exchangeCode(code:string){const r=await fetch('https://github.com/login/oauth/access_token',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({client_id:process.env.GITHUB_CLIENT_ID,client_secret:process.env.GITHUB_CLIENT_SECRET,code,redirect_uri:process.env.GITHUB_REDIRECT_URI})});if(!r.ok)throw Error('OAuth exchange failed');return r.json() as Promise<{access_token:string}>}
